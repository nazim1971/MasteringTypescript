"use strict";
// Basic Data Types
// String type
let myName = "Nazim";
// Number type
let roll = 123;
// Boolean type
let isNazim = true;
// Undefined type
let x = undefined;
// Null type
let y = null;
// Assigning a value to a declared variable
let N;
N = 36; // Always try to declare and assign values separately when possible for clarity
// Array of strings
let friends = ['Miko', 'Monika'];
// Array of numbers
let eligibleRollList = [2, 6];
eligibleRollList.push(12); // Using the `push` method to add values
// Tuple type: fixed-size array with specified types
let coordinates = [1, 7]; // Represents a point in 2D space (x, y)
let ageName = [50, "Mr. Nazim", true]; // Tuple for age, name, and a boolean flag
